{
  "name": "jsnotes",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "vercel-build": "npm run build && mv .vpress/dist dist"
  },
  "devDependencies": {
    "vuepress": "^0.14.4"
  }
}
